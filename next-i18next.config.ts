// next-i18next.config.js
const nextI18NextConfig = {
  i18n: {
    locales: ["en", "bn"], // List of supported languages
    defaultLocale: "en", // Default language
  },
};

export default nextI18NextConfig;
